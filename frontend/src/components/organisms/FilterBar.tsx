"use client";
import { FormControl, InputLabel, MenuItem, Select } from "@mui/material";

export default function FilterBar({ filter, setFilter }: { filter?: string; setFilter?: (s:string)=>void }) {
  return (
    <div>
     
    </div>
  );
}

